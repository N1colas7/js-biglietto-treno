const km = parseInt ( prompt('Quanti chilometri vuoi percorrere?'));
console.log( km );
const eta = parseInt ( prompt('Quanti anni hai?'));
console.log( eta );

if ( 'eta' >= 18 && 'eta' <= 63 ) {
   
}else if ( 'eta' <= 17 ){
    
}